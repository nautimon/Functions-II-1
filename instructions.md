Write a similar function to the last exercise, however, this time write it as a function expression instead of a declaration (or vice versa, if you did a function expression last time). As a reminder, a function expression stores an unnamed (anonymous) function into a variable.

As a reminder, this should do the same things as the last exercise. When we call:

```
sayHi()
```

it will log out "Hi there!".


**Note**: Feel free to reference your code from Part 1 to complete the body of your function.